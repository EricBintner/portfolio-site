
var ClassUtil_cls = Class.extend({
	///////////////////////////////////////////////////////////////////////////////
	///////////////////////////////////////////////////////////////////////////////
	//static methods
	///////////////////////////////////////////////////////////////////////////////
	///////////////////////////////////////////////////////////////////////////////
	isClass:function(obj) {
		return (String(obj.constructor).indexOf("Class") > 0) ? true : false;
	},
	//-------------------------------------------------------------------------------------
	isJohnClass:function(obj) {
		return (obj.toString().indexOf("// All construction is actually done in the init method") > 0) ? true : false;
	}
});
var ClassUtil = new ClassUtil_cls();
